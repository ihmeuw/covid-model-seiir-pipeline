data:
  regression_version: 'best'
  output_root: ''
scenarios:
  baseline:
    beta_scaling_window: -1
    algorithm: 'RK45'  # Later also 'mattingly', 'RK45-loop', etc.
    covariates:
      testing: 'reference'
      mobility: 'lift'
      proportion_over_1k: 'reference'
      temperature: 'reference'
  testing_optimistic:
    beta_scaling_window: -1
    algorithm: 'RK45'
    covariates:
      testing: 'optimistic'
      mobility: 'lift'
      proportion_over_1k: 'reference'
      temperature: 'reference'



